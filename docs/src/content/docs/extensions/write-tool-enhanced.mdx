---
title: write-tool-enhanced
description: See every line as it's written to disk.
---

When the agent writes a file, the final tool result shows the full
written content with a summary footer, instead of only a byte-count
success message.

The LLM receives the standard short confirmation (it already knows
what it wrote). Only the visual display is enhanced.
